import { StyleSheet } from "react-native";

const devolucionStyles = StyleSheet.create({
container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export { devolucionStyles }
